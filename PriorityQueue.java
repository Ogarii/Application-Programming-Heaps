package org.example;

public interface PriorityQueue {
    void insert(int value); // Method to insert a value into the priority queue
    int extract(); // Method to extract the highest or lowest priority value
    boolean isEmpty(); // Method to check if the priority queue is empty

}
